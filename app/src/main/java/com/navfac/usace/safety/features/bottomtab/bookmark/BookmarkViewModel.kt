package com.navfac.usace.safety.features.bottomtab.bookmark

import com.navfac.usace.safety.base.platform.BaseViewModel

class BookmarkViewModel : BaseViewModel() {

//    fun clearBookmarkAllItem(context: Context, onDeleteButtonClick: () -> Unit) {
//        val realm: Realm = Realm.getDefaultInstance()
//        showMessageDialogWithAction(context, "Are you sure you want to clear All bookmark item?", "Yes", "Not Now") {
//            val realmResults: RealmResults<BookmarkModel> = realm.where(BookmarkModel::class.java).findAll()
//            realm.executeTransaction {
//                realmResults.deleteAllFromRealm()
//                showToast(context, "All Bookmark Cleared!")
//                onDeleteButtonClick.invoke()
//            }
//        }
//    }

}